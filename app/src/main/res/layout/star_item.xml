<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/img"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:src="@mipmap/ic_launcher" /> <!-- Utilisez une image par défaut -->

    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/img"
        android:layout_marginStart="16dp"
        android:text="Star Name"
        android:textSize="18sp" />

    <RatingBar
        android:id="@+id/stars"
        style="?android:attr/ratingBarStyleIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/name"
        android:layout_marginTop="8dp"
        android:layout_toEndOf="@id/img"
        android:isIndicator="false"
        android:numStars="5"
        android:progressTint="#090909"
        android:rating="0"
        android:stepSize="0.5" /> <!-- Assurez-vous que ce paramètre est false -->
    <ImageView
        android:id="@+id/iconshare"
        android:layout_width="match_parent"
        android:layout_height="121dp"
        android:layout_marginStart="285dp"
        android:layout_marginTop="35dp"
        android:clickable="true"
        android:contentDescription="@drawable/shareacteur"
        android:focusable="true"
        android:src="@drawable/shareacteur"
        app:srcCompat="@drawable/shareacteur"
        tools:srcCompat="@drawable/shareacteur" />

</RelativeLayout>
